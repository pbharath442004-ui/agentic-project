# Frequently Asked Questions — Datazoic Pay
One entry per Q&A (auto-generated sample corpus; 120 entries).

## FAQ-001 · How do I create an invoice via API?
**Short answer:** Yes — this is handled automatically in the Invoicing module and can also be driven via the API.

In detail: the Invoicing module processes this with a typical latency of 117 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 28 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-002 · How long are drafts kept?
**Short answer:** Yes — this is handled automatically in the Invoicing module and can also be driven via the API.

In detail: the Invoicing module processes this with a typical latency of 92 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 4 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-003 · Can invoices be multi-currency?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Invoicing module processes this with a typical latency of 31 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 25 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-004 · How are PDFs generated?
**Short answer:** Exactly — the Invoicing module keeps a full audit trail for every step.

In detail: the Invoicing module processes this with a typical latency of 58 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-005 · When are reminders sent?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Invoicing module processes this with a typical latency of 111 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 8 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-006 · How do invoice summaries work?
**Short answer:** Exactly — the Invoicing module keeps a full audit trail for every step.

In detail: the Invoicing module processes this with a typical latency of 16 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 12 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `invoicing` group for exact field names.

## FAQ-007 · What payment methods are supported?
**Short answer:** Exactly — the Payments module keeps a full audit trail for every step.

In detail: the Payments module processes this with a typical latency of 48 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-008 · How fast are refunds processed?
**Short answer:** It works through the Payments settings; the default can be overridden per record.

In detail: the Payments module processes this with a typical latency of 11 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-009 · What happens when a payment fails?
**Short answer:** It works through the Payments settings; the default can be overridden per record.

In detail: the Payments module processes this with a typical latency of 91 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-010 · How do I capture a payment?
**Short answer:** Yes — this is handled automatically in the Payments module and can also be driven via the API.

In detail: the Payments module processes this with a typical latency of 102 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-011 · Can I refund part of a payment?
**Short answer:** It works through the Payments settings; the default can be overridden per record.

In detail: the Payments module processes this with a typical latency of 38 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 6 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-012 · How are fees calculated on payments?
**Short answer:** It works through the Payments settings; the default can be overridden per record.

In detail: the Payments module processes this with a typical latency of 74 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 9 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payments` group for exact field names.

## FAQ-013 · What is a dispute?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 26 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 12 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-014 · How long do I have to respond?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 16 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-015 · What evidence helps?
**Short answer:** Exactly — the Disputes & Chargebacks module keeps a full audit trail for every step.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 22 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-016 · Do I pay chargeback fees if I win?
**Short answer:** It works through the Disputes & Chargebacks settings; the default can be overridden per record.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 35 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-017 · Can disputes auto-close?
**Short answer:** Exactly — the Disputes & Chargebacks module keeps a full audit trail for every step.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 70 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-018 · How do I track a dispute?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Disputes & Chargebacks module processes this with a typical latency of 87 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 12 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `disputes-&-chargebacks` group for exact field names.

## FAQ-019 · What is the difference between GMV and revenue?
**Short answer:** It works through the Reporting settings; the default can be overridden per record.

In detail: the Reporting module processes this with a typical latency of 118 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-020 · Which periods can I report on?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Reporting module processes this with a typical latency of 77 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 8 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-021 · How do I export reports?
**Short answer:** It works through the Reporting settings; the default can be overridden per record.

In detail: the Reporting module processes this with a typical latency of 75 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 14 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-022 · Are reports real-time?
**Short answer:** Exactly — the Reporting module keeps a full audit trail for every step.

In detail: the Reporting module processes this with a typical latency of 81 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 15 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-023 · Can I schedule reports?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Reporting module processes this with a typical latency of 19 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-024 · Where do fee numbers come from?
**Short answer:** It works through the Reporting settings; the default can be overridden per record.

In detail: the Reporting module processes this with a typical latency of 51 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 16 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reporting` group for exact field names.

## FAQ-025 · Where is customer data stored?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Customer Management module processes this with a typical latency of 12 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 29 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-026 · How do I export customer data?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Customer Management module processes this with a typical latency of 30 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 4 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-027 · Can I tag customers?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Customer Management module processes this with a typical latency of 19 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 6 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-028 · How are balances calculated?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Customer Management module processes this with a typical latency of 64 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-029 · What happens when a customer is deleted?
**Short answer:** Yes — this is handled automatically in the Customer Management module and can also be driven via the API.

In detail: the Customer Management module processes this with a typical latency of 26 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 18 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-030 · How do I link a user to invoices?
**Short answer:** Exactly — the Customer Management module keeps a full audit trail for every step.

In detail: the Customer Management module processes this with a typical latency of 6 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 15 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `customer-management` group for exact field names.

## FAQ-031 · How do webhooks work?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Webhooks & Events module processes this with a typical latency of 100 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-032 · What events exist?
**Short answer:** Exactly — the Webhooks & Events module keeps a full audit trail for every step.

In detail: the Webhooks & Events module processes this with a typical latency of 38 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 4 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-033 · How do I verify signatures?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Webhooks & Events module processes this with a typical latency of 67 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-034 · What happens if my endpoint is down?
**Short answer:** Yes — this is handled automatically in the Webhooks & Events module and can also be driven via the API.

In detail: the Webhooks & Events module processes this with a typical latency of 50 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 30 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-035 · How do I test a webhook?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Webhooks & Events module processes this with a typical latency of 85 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 20 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-036 · Are deliveries guaranteed?
**Short answer:** Yes — this is handled automatically in the Webhooks & Events module and can also be driven via the API.

In detail: the Webhooks & Events module processes this with a typical latency of 15 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 5 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `webhooks-&-events` group for exact field names.

## FAQ-037 · How often do rates update?
**Short answer:** It works through the FX Engine settings; the default can be overridden per record.

In detail: the FX Engine module processes this with a typical latency of 104 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 13 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-038 · Can I lock a rate?
**Short answer:** It works through the FX Engine settings; the default can be overridden per record.

In detail: the FX Engine module processes this with a typical latency of 52 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 6 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-039 · Which pairs are supported?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the FX Engine module processes this with a typical latency of 105 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 24 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-040 · What is the FX spread?
**Short answer:** It works through the FX Engine settings; the default can be overridden per record.

In detail: the FX Engine module processes this with a typical latency of 46 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 13 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-041 · How long is rate history kept?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the FX Engine module processes this with a typical latency of 45 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 8 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-042 · Are there conversion limits?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the FX Engine module processes this with a typical latency of 75 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 30 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fx-engine` group for exact field names.

## FAQ-043 · How does proration work?
**Short answer:** Yes — this is handled automatically in the Subscriptions & Billing module and can also be driven via the API.

In detail: the Subscriptions & Billing module processes this with a typical latency of 35 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 2 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-044 · What is dunning?
**Short answer:** Exactly — the Subscriptions & Billing module keeps a full audit trail for every step.

In detail: the Subscriptions & Billing module processes this with a typical latency of 77 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 26 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-045 · How do I migrate a customer to a new plan?
**Short answer:** Exactly — the Subscriptions & Billing module keeps a full audit trail for every step.

In detail: the Subscriptions & Billing module processes this with a typical latency of 32 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 16 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-046 · What happens at cancellation?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Subscriptions & Billing module processes this with a typical latency of 98 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 6 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-047 · How is usage-based billing metered?
**Short answer:** Yes — this is handled automatically in the Subscriptions & Billing module and can also be driven via the API.

In detail: the Subscriptions & Billing module processes this with a typical latency of 79 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 21 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-048 · Can trials convert automatically?
**Short answer:** It works through the Subscriptions & Billing settings; the default can be overridden per record.

In detail: the Subscriptions & Billing module processes this with a typical latency of 93 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 8 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `subscriptions-&-billing` group for exact field names.

## FAQ-049 · What is tokenization?
**Short answer:** Exactly — the Card Processing module keeps a full audit trail for every step.

In detail: the Card Processing module processes this with a typical latency of 61 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 21 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-050 · When is 3-D Secure required?
**Short answer:** Exactly — the Card Processing module keeps a full audit trail for every step.

In detail: the Card Processing module processes this with a typical latency of 10 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 28 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-051 · What does an AVS mismatch mean?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Card Processing module processes this with a typical latency of 29 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-052 · Which card brands are supported?
**Short answer:** Exactly — the Card Processing module keeps a full audit trail for every step.

In detail: the Card Processing module processes this with a typical latency of 9 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 27 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-053 · How are declines reported?
**Short answer:** Yes — this is handled automatically in the Card Processing module and can also be driven via the API.

In detail: the Card Processing module processes this with a typical latency of 41 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 3 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-054 · How is card data protected?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Card Processing module processes this with a typical latency of 95 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 14 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `card-processing` group for exact field names.

## FAQ-055 · How long do transfers take?
**Short answer:** It works through the Bank Transfers settings; the default can be overridden per record.

In detail: the Bank Transfers module processes this with a typical latency of 61 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 1 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-056 · What is the transfer limit?
**Short answer:** Yes — this is handled automatically in the Bank Transfers module and can also be driven via the API.

In detail: the Bank Transfers module processes this with a typical latency of 53 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 10 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-057 · How do I track a transfer?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Bank Transfers module processes this with a typical latency of 107 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-058 · Which currencies are supported?
**Short answer:** Yes — this is handled automatically in the Bank Transfers module and can also be driven via the API.

In detail: the Bank Transfers module processes this with a typical latency of 30 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 16 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-059 · What happens on a failed transfer?
**Short answer:** Exactly — the Bank Transfers module keeps a full audit trail for every step.

In detail: the Bank Transfers module processes this with a typical latency of 46 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-060 · How are refunds on transfers handled?
**Short answer:** It works through the Bank Transfers settings; the default can be overridden per record.

In detail: the Bank Transfers module processes this with a typical latency of 73 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 30 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `bank-transfers` group for exact field names.

## FAQ-061 · When are payouts processed?
**Short answer:** Yes — this is handled automatically in the Payouts module and can also be driven via the API.

In detail: the Payouts module processes this with a typical latency of 82 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 20 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-062 · Why is my payout held?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Payouts module processes this with a typical latency of 97 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-063 · How are payout batches formed?
**Short answer:** Exactly — the Payouts module keeps a full audit trail for every step.

In detail: the Payouts module processes this with a typical latency of 89 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 10 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-064 · Can I change payout routing?
**Short answer:** It works through the Payouts settings; the default can be overridden per record.

In detail: the Payouts module processes this with a typical latency of 66 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-065 · What triggers a payout hold?
**Short answer:** Yes — this is handled automatically in the Payouts module and can also be driven via the API.

In detail: the Payouts module processes this with a typical latency of 115 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 11 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-066 · How do I reconcile payouts?
**Short answer:** Exactly — the Payouts module keeps a full audit trail for every step.

In detail: the Payouts module processes this with a typical latency of 33 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `payouts` group for exact field names.

## FAQ-067 · How is tax calculated?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Tax Engine module processes this with a typical latency of 23 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 22 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-068 · Which taxes are supported?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Tax Engine module processes this with a typical latency of 79 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 14 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-069 · How do I validate a tax ID?
**Short answer:** Exactly — the Tax Engine module keeps a full audit trail for every step.

In detail: the Tax Engine module processes this with a typical latency of 35 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-070 · How do I export filing data?
**Short answer:** It works through the Tax Engine settings; the default can be overridden per record.

In detail: the Tax Engine module processes this with a typical latency of 38 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 4 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-071 · How do cross-border rules apply?
**Short answer:** Yes — this is handled automatically in the Tax Engine module and can also be driven via the API.

In detail: the Tax Engine module processes this with a typical latency of 118 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-072 · What is a tax invoice?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Tax Engine module processes this with a typical latency of 115 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 27 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `tax-engine` group for exact field names.

## FAQ-073 · How are risk scores computed?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Fraud & Risk module processes this with a typical latency of 29 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-074 · What is a velocity rule?
**Short answer:** Exactly — the Fraud & Risk module keeps a full audit trail for every step.

In detail: the Fraud & Risk module processes this with a typical latency of 34 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 18 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-075 · What does a 'review' decision mean?
**Short answer:** Yes — this is handled automatically in the Fraud & Risk module and can also be driven via the API.

In detail: the Fraud & Risk module processes this with a typical latency of 74 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-076 · How do blocklists work?
**Short answer:** Yes — this is handled automatically in the Fraud & Risk module and can also be driven via the API.

In detail: the Fraud & Risk module processes this with a typical latency of 80 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-077 · Can I appeal a fraud flag?
**Short answer:** Exactly — the Fraud & Risk module keeps a full audit trail for every step.

In detail: the Fraud & Risk module processes this with a typical latency of 91 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 3 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-078 · How are devices fingerprinted?
**Short answer:** Yes — this is handled automatically in the Fraud & Risk module and can also be driven via the API.

In detail: the Fraud & Risk module processes this with a typical latency of 115 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `fraud-&-risk` group for exact field names.

## FAQ-079 · Where are secrets stored?
**Short answer:** Exactly — the Vault module keeps a full audit trail for every step.

In detail: the Vault module processes this with a typical latency of 18 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 15 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-080 · How often must keys rotate?
**Short answer:** Exactly — the Vault module keeps a full audit trail for every step.

In detail: the Vault module processes this with a typical latency of 26 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-081 · Who can read a secret?
**Short answer:** Yes — this is handled automatically in the Vault module and can also be driven via the API.

In detail: the Vault module processes this with a typical latency of 22 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 12 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-082 · How do I audit secret access?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Vault module processes this with a typical latency of 35 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 2 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-083 · Can secrets be versioned?
**Short answer:** It works through the Vault settings; the default can be overridden per record.

In detail: the Vault module processes this with a typical latency of 6 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-084 · What happens on rotation?
**Short answer:** It works through the Vault settings; the default can be overridden per record.

In detail: the Vault module processes this with a typical latency of 43 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 4 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `vault` group for exact field names.

## FAQ-085 · How does double-entry posting work?
**Short answer:** It works through the Ledger & Accounting settings; the default can be overridden per record.

In detail: the Ledger & Accounting module processes this with a typical latency of 118 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 3 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-086 · What is period close?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Ledger & Accounting module processes this with a typical latency of 19 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 30 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-087 · How do I correct a journal entry?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Ledger & Accounting module processes this with a typical latency of 51 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 24 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-088 · How are balances computed?
**Short answer:** Yes — this is handled automatically in the Ledger & Accounting module and can also be driven via the API.

In detail: the Ledger & Accounting module processes this with a typical latency of 110 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 9 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-089 · Which formats can I export?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Ledger & Accounting module processes this with a typical latency of 52 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-090 · What is the chart of accounts?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Ledger & Accounting module processes this with a typical latency of 10 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 27 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `ledger-&-accounting` group for exact field names.

## FAQ-091 · Which channels are supported?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Notifications module processes this with a typical latency of 50 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 18 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-092 · How do templates work?
**Short answer:** It works through the Notifications settings; the default can be overridden per record.

In detail: the Notifications module processes this with a typical latency of 19 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 2 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-093 · What are quiet hours?
**Short answer:** Exactly — the Notifications module keeps a full audit trail for every step.

In detail: the Notifications module processes this with a typical latency of 50 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 7 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-094 · How do I check delivery logs?
**Short answer:** Exactly — the Notifications module keeps a full audit trail for every step.

In detail: the Notifications module processes this with a typical latency of 112 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-095 · Can I suppress notifications?
**Short answer:** Exactly — the Notifications module keeps a full audit trail for every step.

In detail: the Notifications module processes this with a typical latency of 106 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 1 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-096 · How fast are SMS delivered?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Notifications module processes this with a typical latency of 14 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 26 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `notifications` group for exact field names.

## FAQ-097 · What documents are accepted?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Identity & KYC module processes this with a typical latency of 24 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 18 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-098 · How long does verification take?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Identity & KYC module processes this with a typical latency of 112 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 5 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-099 · What is rescreening?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Identity & KYC module processes this with a typical latency of 93 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 25 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-100 · How are biometrics stored?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Identity & KYC module processes this with a typical latency of 6 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 1 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-101 · What does 'under_review' mean?
**Short answer:** Exactly — the Identity & KYC module keeps a full audit trail for every step.

In detail: the Identity & KYC module processes this with a typical latency of 67 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-102 · How do I get KYC status via API?
**Short answer:** Yes — this is handled automatically in the Identity & KYC module and can also be driven via the API.

In detail: the Identity & KYC module processes this with a typical latency of 107 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 27 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `identity-&-kyc` group for exact field names.

## FAQ-103 · How does seller onboarding work?
**Short answer:** Exactly — the Marketplace module keeps a full audit trail for every step.

In detail: the Marketplace module processes this with a typical latency of 28 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 20 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-104 · When are settlements run?
**Short answer:** Exactly — the Marketplace module keeps a full audit trail for every step.

In detail: the Marketplace module processes this with a typical latency of 25 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-105 · How are marketplace fees set?
**Short answer:** Yes — this is handled automatically in the Marketplace module and can also be driven via the API.

In detail: the Marketplace module processes this with a typical latency of 34 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 28 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-106 · What is buyer protection?
**Short answer:** It works through the Marketplace settings; the default can be overridden per record.

In detail: the Marketplace module processes this with a typical latency of 47 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-107 · How are claims resolved?
**Short answer:** Yes — this is handled automatically in the Marketplace module and can also be driven via the API.

In detail: the Marketplace module processes this with a typical latency of 119 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 5 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-108 · How are seller payouts triggered?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Marketplace module processes this with a typical latency of 26 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 27 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `marketplace` group for exact field names.

## FAQ-109 · How does auto-reconciliation match items?
**Short answer:** Exactly — the Reconciliation module keeps a full audit trail for every step.

In detail: the Reconciliation module processes this with a typical latency of 47 ms and full API support. Timelines to remember: review turnaround is 4 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 19 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-110 · Which statement formats are supported?
**Short answer:** Yes — this is handled automatically in the Reconciliation module and can also be driven via the API.

In detail: the Reconciliation module processes this with a typical latency of 50 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 11 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-111 · What is a mismatch?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Reconciliation module processes this with a typical latency of 79 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 16 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-112 · How do I audit a reconciliation run?
**Short answer:** Exactly — the Reconciliation module keeps a full audit trail for every step.

In detail: the Reconciliation module processes this with a typical latency of 7 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 8 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-113 · How often should I reconcile?
**Short answer:** It works through the Reconciliation settings; the default can be overridden per record.

In detail: the Reconciliation module processes this with a typical latency of 10 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 21 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-114 · What happens to unmatched items?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Reconciliation module processes this with a typical latency of 39 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 13 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `reconciliation` group for exact field names.

## FAQ-115 · When is sanctions screening run?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Compliance Center module processes this with a typical latency of 69 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 9 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-116 · How are AML cases prioritized?
**Short answer:** It works through the Compliance Center settings; the default can be overridden per record.

In detail: the Compliance Center module processes this with a typical latency of 78 ms and full API support. Timelines to remember: review turnaround is 1 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 17 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-117 · What filings are automated?
**Short answer:** Yes — this is handled automatically in the Compliance Center module and can also be driven via the API.

In detail: the Compliance Center module processes this with a typical latency of 76 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 29 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-118 · How do I export the audit trail?
**Short answer:** Yes — this is handled automatically in the Compliance Center module and can also be driven via the API.

In detail: the Compliance Center module processes this with a typical latency of 104 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 14 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-119 · How are consents recorded?
**Short answer:** It works through the Compliance Center settings; the default can be overridden per record.

In detail: the Compliance Center module processes this with a typical latency of 106 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 10 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-120 · What is in a compliance report?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Compliance Center module processes this with a typical latency of 92 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 3 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `compliance-center` group for exact field names.

## FAQ-121 · How do I create a ticket?
**Short answer:** It works through the Support settings; the default can be overridden per record.

In detail: the Support module processes this with a typical latency of 99 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 12 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.

## FAQ-122 · What are the priority levels?
**Short answer:** Exactly — the Support module keeps a full audit trail for every step.

In detail: the Support module processes this with a typical latency of 116 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 18 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.

## FAQ-123 · How fast will someone respond?
**Short answer:** This depends on your plan, but the standard behavior is documented below.

In detail: the Support module processes this with a typical latency of 12 ms and full API support. Timelines to remember: review turnaround is 3 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 23 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.

## FAQ-124 · How does SLA tracking work?
**Short answer:** Exactly — the Support module keeps a full audit trail for every step.

In detail: the Support module processes this with a typical latency of 118 ms and full API support. Timelines to remember: review turnaround is 5 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 26 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.

## FAQ-125 · How do I submit feedback?
**Short answer:** It works through the Support settings; the default can be overridden per record.

In detail: the Support module processes this with a typical latency of 52 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 29 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.

## FAQ-126 · Who can raise a ticket?
**Short answer:** It works through the Support settings; the default can be overridden per record.

In detail: the Support module processes this with a typical latency of 49 ms and full API support. Timelines to remember: review turnaround is 2 business days, records are kept per the applicable retention policy, and any status change emits a webhook event within 5 minutes. For edge cases, raise a support ticket (Support module) — the ticket is linked to the underlying record automatically. See the API reference entries for the `support` group for exact field names.
